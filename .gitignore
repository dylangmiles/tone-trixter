# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Local bundler install (GitHub Pages builds with its own gem set)
vendor/
.bundle/
Gemfile.lock
# macOS
.DS_Store

# Claude Code session data
.claude/
